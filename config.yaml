# Polaris 防漂移系统 · 根统一配置（P-05：阈值与 detector.py 默认一致 0.15/0.25/0.30）
version: "1.0"

anti_drift:
  thresholds:
    green: 0.15
    gray: 0.25
    yellow: 0.30
    red: 0.30

server:
  host: "0.0.0.0"
  port: 5052

# M3 P-CODE-003：订阅计费（价格 / 限额 / trial / webhook 签名的权威来源）
billing:
  trial_days: 14
  currency: "CNY"
  webhook_secret: "polaris_dev_whsec_20260818"
  plans:
    free:
      price_monthly: 0
      quota:
        detection: 100
        snapshot: 10
        alert_push: 5
    pro:
      price_monthly: 49
      quota:
        detection: 5000
        snapshot: 500
        alert_push: 200
    enterprise:
      price_monthly: 199
      quota:
        detection: 50000
        snapshot: 5000
        alert_push: 2000
  overage_rate:
    detection: 0.01
    snapshot: 0.05
    alert_push: 0.02

logging:
  level: "INFO"
  polaris_log: "./polaris.log"

# v0.1.3 signal_enhancer 配置（瞬 2026-08-20 CI fix）
signal_enhancer:
  enabled: true
  boost_coefficient: 0.12
  active_collapse_bonus: 0.12
  single_level_cap: true

# Polaris 防漂移系统 · 依赖清单（v0.3.0）
flask>=3.0
sqlalchemy>=2.0
pyyaml>=6.0
pytest>=8.0
requests>=2.31

# 认证依赖（auth.py，M3 部署复核提示补装）
bcrypt>=4.0
PyJWT>=2.8

# MemGuard 依赖（M1 决策：不复制源码，以依赖引入）
# 待 memguard 发布为 pip 包后改为：memguard>=x.y.z
# 当前开发期安装方式：pip install -e <path-to-memguard-source>

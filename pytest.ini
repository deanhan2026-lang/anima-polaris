# pytest 配置：排除脚本式集成测试（手动运行，非 pytest 单元测试）
# test_e2e / test_quick / test_goal_integration 为顶层执行脚本，
# 被 pytest 收集时会破坏 capture（sys.stdout 替换 / sys.exit），导致 CI 崩溃
[pytest]
addopts = --ignore=tests/test_e2e.py --ignore=tests/test_quick.py --ignore=tests/test_goal_integration.py
